# Todo List

- Remove or implement currently unused `borderStyle` / `BorderType` handling in dialog rendering.
- Add configurable character limit support for `TextLineDialog`.
- Add validation hooks for `TextLineDialog`, for example required value or custom validation rule.
- Add dedicated user documentation for `NavigationToolbar` and `NavigationToolbarRenderer`.
- Decide whether dialog configuration should also be loadable from `.properties` files in addition to Java builders.
- Add automated tests for dialog builders and UI area composition.
